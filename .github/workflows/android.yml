name: Android CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Gradle 9.5
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "9.5.0"

      - name: Decode stable signing key
        run: |
          base64 -d .github/keys/audoiboo-release.keystore.b64 > .github/keys/audoiboo-release.jks
          keytool -list -keystore .github/keys/audoiboo-release.jks -storepass Audoiboo2026 -alias audoiboo >/dev/null

      - name: Build release APK
        run: gradle :app:assembleRelease --stacktrace

      - name: Rename APK
        run: cp app/build/outputs/apk/release/app-release.apk Audoiboo-Tracker-1.0.1.apk

      - name: Upload workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: Audoiboo-Tracker-1.0.1
          path: Audoiboo-Tracker-1.0.1.apk

      - name: Publish stable release
        if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release delete v1.0.1 --yes --cleanup-tag || true
          gh release create v1.0.1 Audoiboo-Tracker-1.0.1.apk --target "$GITHUB_SHA" --title "Audoiboo Tracker 1.0.1" --notes "Bugfix release: book URLs are resolved to their Audioboo cycle before syncing, embedded-browser additions now resolve the correct cycle name/link, and automatic archive discovery has per-task completion guards and timeouts to prevent endless syncing."
